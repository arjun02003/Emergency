# Example frontend environment variables (Vite)
# Copy values into a local `.env` when building or set them in CI as secrets.

# URL of the backend API (used during build/runtime by frontend)
VITE_API_URL=http://localhost:5000

# Mapbox token for map features
VITE_MAPBOX_TOKEN=your_mapbox_token_here
